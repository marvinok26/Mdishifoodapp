import React from 'react';
import SearchBar from './SearchBar'; // Check the relative path

const App = () => {
  return (
    <div>
      <h1>Food Search App</h1>
      <SearchBar />
    </div>
  );
};

export default App;
